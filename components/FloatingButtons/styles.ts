import { styled } from "@mui/material/styles";

export const Wrapper = styled("div")({
  position: 'fixed',
  bottom: '2rem',
  right: '2rem',
  display: 'flex',
  alignItems: 'center',
  gap: '1rem'
});
